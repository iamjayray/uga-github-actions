pipeline {
    agent any
    stages {
        stage('Print hello') {
            steps { 
                // Print 'Hello world!' to the console
                echo 'Hello world!'
            }
        }
    }
}